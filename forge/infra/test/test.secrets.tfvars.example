# Copy to test.secrets.tfvars and fill in the admin token.
# This file is gitignored — never commit the real version.
#
# Apply with both var-files:
#   terraform apply -var-file=test.tfvars -var-file=test.secrets.tfvars

forgejo_admin_token = "YOUR_ADMIN_TOKEN_HERE"
